import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  inputs: {
    marginTop: 40,
    marginBottom: 20,
    gap: 20,
  },
  input: {
    backgroundColor: '#121214',
    borderColor: '#121214',
  },
  inputText: {
    color: '#E1E1E6',
  },
});
